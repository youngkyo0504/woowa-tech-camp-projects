import { css } from 'styled-components';

const borderRadiusVariable = css`
  --rounded-sm: 4px;
  --rounded-md: 8px;
  --rounded-lg: 12px;
  --rounded-xl: 16px;
`;

export default borderRadiusVariable;
