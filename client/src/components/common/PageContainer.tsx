import { padding } from '@constants/padding';
import styled from 'styled-components';

const PageContainer = styled.div`
  padding: ${padding.pageTop} ${padding.pageSide};
  padding-bottom: 0;
`;

export default PageContainer;
